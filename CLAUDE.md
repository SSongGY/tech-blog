# 기술 블로그 작성 규칙

이 저장소는 매일 여러 편의 IT 기술 글을 생성·보관한다. 글은 마크다운이 원본이고,
플랫폼(티스토리/네이버)에는 변환본을 붙여넣어 발행한다.

## 트랙 구성

**하루 4회, 총 10편.**

| 시각 | 루틴 | 편수 | 내용 |
|---|---|---|---|
| 07:00 | `tech-blog-daily` | 3편 | 아래 트랙 구성 |
| 15:00 | `tech-blog-exam` | 단답형 2 / 논술형 1 | 기출 답안 → §11 |
| 17:00 | `tech-blog-concept` | 2편 | 15시 답안에서 나온 개념을 `pe` 글로 → §7 |
| 21:00 | `tech-blog-daily` | 3편 | 아래 트랙 구성 |

07:00·21:00 회차의 구성은 다음과 같다.

| 트랙 | 편수 | 내용 | 분량 | 기본 검증 |
|---|---|---|---|---|
| **DB 문법** `basics` | 1편 고정 | DB 기본 문법. SQLite로 누구나 재현 가능한 표준 SQL | 1,200~2,500자 | `executed` |
| **기술사** `pe` | 1편 고정 | 정보관리기술사 시험 과목 기술 개념·정의 | 1,500~3,000자 | `manual-only` |
| **DB 기능** `product` | 세 자리를 | DB 제품의 기능별 사용법·문법 | 1,500~3,800자 | 환경에 따라 |
| **리눅스** `linux` | 돌아가며 | 서버 운영에서 실제로 치는 명령 | 1,200~2,800자 | 환경에 따라 |
| **일반** `general` | 1편 | 그 외 IT 기술. DB·백엔드 60% + IT 전반 40% | 1,800~3,500자 | `executed` |

세 번째 자리는 `product` / `linux` / `general` 중 **지금까지 가장 적게 쓴 트랙**이
가져간다. `blog.py`의 `daily_plan()`이 판단한다. 한 트랙이 소진되면 후보에서 빠지고
나머지가 메우므로 **편수는 줄지 않는다.**

5편으로 돌려 본 결과 한 회차에 48분이 걸렸다. 3편으로 줄이고 회차를 늘린 이유다.

15시·17시는 정보관리기술사 준비용이다. 기출 답안을 쓰고(15시), 그 답안에서 나온
개념을 두 시간 뒤에 개념 글로 정리한다(17시). **답안과 개념 글을 한 회차에 몰지 않는다** —
둘 다 얕아진다. 이어주는 것은 `--origin exam` 표시다. §11을 본다.

**편수를 채우려고 기준을 낮추지 않는다.** 규칙대로 못 쓰겠으면 쓸 수 있는 만큼만 쓰고,
몇 편을 왜 못 썼는지 보고한다. 기준을 어긴 3편보다 기준을 지킨 2편이 낫다.

## 1. 글의 정체성

- **독자**: 트랙마다 다르다.
  - `basics`: SQL을 막 배우는 입문~1년차. 용어를 처음부터 설명한다
  - `product` / `general`: 실무 2~8년차 백엔드·DB 엔지니어. 용어 설명은 최소화한다
  - `pe`: 정보관리기술사를 준비하는 사람. **답안에 쓸 수 있는 형태**로 정리한다
- **톤**: 담백한 평서체(`~한다`, `~된다`). 감탄사·이모지·"여러분"·"~해보아요" 금지.
- **비유로 동작을 설명하지 않는다.** 실제로 무슨 일이 일어나는지를 가리키는 동사를 쓴다.
  비유는 읽는 사람이 한 번 더 번역해야 하고, 번역이 사람마다 갈리면 뜻이 흐려진다.

  | 쓰지 않는다 | 쓴다 |
  |---|---|
  | 한 사이클이 사람 개입 없이 **닫힌다** | 한 사이클이 사람 개입 없이 **수행된다** |
  | 조치까지 자동으로 **닫는다** | 조치까지 설비가 **스스로 수행한다** |
  | **예외의 꼬리**가 발목을 잡는다 | **드물게 일어나는 예외**가 남는다 |

  판별법은 간단하다. **그 동사를 사전 뜻 그대로 읽어도 말이 되는가.**
  "사이클이 닫힌다"는 사전 뜻으로 읽으면 무슨 일인지 알 수 없다. 그러면 바꾼다.
  다만 해당 분야에서 **용어로 굳은 말**(폴백, 롤백, 핸드셰이크, 커버링 인덱스)은 비유가
  아니라 이름이므로 그대로 쓴다.
- **분량**: 트랙별로 다르다(위 표). `blog.py lint`가 트랙 기준으로 센다.
  짧으면 얕고, 길면 안 읽는다.
- **핵심 원칙**: *검증되지 않은 문장은 쓰지 않는다.* 코드 예제는 반드시 실행해서 출력까지 확인한 뒤 싣는다.
  실행할 수 없는 예제(상용 DB, 유료 서비스 등)는 글 안에 "미검증" 표시를 남긴다.

## 2. 글 구조 (고정 템플릿)

`basics` · `product` · `general` 트랙에 적용된다. `pe` 트랙은 §7의 답안 구조를 쓴다.

1. **들어가며** — **독자가 겪어봤을 법한 일상 장면**으로 시작한다. 3~5문장.
   이론 요약이나 용어 정의로 시작하지 않는다. "무엇을 알아보겠다"는 예고도 쓰지 않는다.
   - 취미 프로젝트든 업무든 **어쩌다 이 상황에 놓이는지**를 먼저 그린다
   - 그 상황에서 **보통 사람들이 택하는 비효율적인 방법**을 짚는다
   - 그 방법이 왜 비싼지를 숫자나 횟수로 드러낸 뒤 본론으로 넘어간다

   예 (git bisect):
   > 개인 취미로든 업무로든 git을 쓰다 보면 커밋이 쌓이다가, 어느 순간 분명 되던 기능이
   > 안 된다는 걸 확인할 때가 있다. 그 사이 커밋은 60개가 넘고, 배포 로그를 봐도 범인이
   > 특정되지 않는다. 이때 대부분은 최근 커밋부터 눈으로 훑기 시작하는데,
   > 이건 최악의 경우 60번을 봐야 하는 선형 탐색이다.

2. **개념** — 무엇인지, 왜 생겼는지. 기존 방식과 무엇이 다른지.
3. **구조** — 도식 최소 1개. 도식 없이 설명되는 주제면 주제 선정이 잘못된 것이다.
4. **동작 원리** — 내부에서 실제로 무슨 일이 일어나는지. 여기서 글의 깊이가 갈린다.
5. **실습 예제** — 실행 가능한 코드 + 실제 출력. `code/` 폴더에 전체 소스를 둔다.
6. **실무에서 주의할 점** — 함정, 안티패턴, 성능 트레이드오프. 최소 3개.
7. **정리** — 3~5줄 요약. 새로운 정보를 여기서 처음 꺼내지 않는다.
8. **참고 자료** — 1차 자료(공식 문서·스펙·논문) 우선. 블로그 인용은 최소화.

## 3. 코드 규칙

- 예제는 글 폴더의 `code/` 아래에 **그 자체로 실행되는 형태**로 둔다.
  의존성이 있으면 `requirements.txt`/`go.mod` 등을 함께 두고, `code/README.md`에 실행 명령을 적는다.
- 변수명은 **언어별 공식 컨벤션**을 따른다 → `references/naming-conventions.md` 참조.
  글에 싣기 전 해당 문서를 확인한다. 임의로 줄이거나 늘리지 않는다.
- 본문에 붙이는 코드 블록은 40줄을 넘기지 않는다. 넘으면 핵심만 잘라 싣고 전체는 `code/`로 링크.
- 주석은 "무엇을"이 아니라 "왜"를 적는다. 자명한 줄에는 주석을 달지 않는다.
- 출력은 실제 실행 결과를 그대로 붙인다. 지어내지 않는다.

## 4. 도식 규칙

**형식은 손으로 작성한 SVG를 쓴다.** 글 폴더의 `fig/` 아래에 두고 `![설명](fig/이름.svg)`로 넣는다.

- Mermaid는 티스토리에서 렌더링되지 않고, PPT 캡처는 diff도 안 되고 자동화도 막힌다.
  SVG는 텍스트라 git diff가 되고 GitHub·Jekyll·티스토리 전부에서 이미지로 뜬다.
- 캔버스는 폭 880 안팎. 배경에 밝은색 `<rect>`를 반드시 깔아 다크 모드에서도 읽히게 한다.
- 글자 크기는 13px 이상. 노드 라벨은 한글, 식별자는 영문. 한 도식에 박스 12개를 넘기지 않는다.
- 색은 의미로만 쓴다: 정상/빠른 경로 `#2d6a4f`, 실패/느린 경로 `#9d0208`, 주의 `#b45309`.
- **작성 후 브라우저로 실제로 열어 글자 잘림·겹침을 확인한다.**

### 출처 표기 (필수)

도식은 **공식 자료를 근거로 그린다.** 추측으로 내부 구조를 그리지 않는다.

- 자료를 찾을 때는 1차 자료를 쓴다: 공식 문서, 파일 포맷 명세, RFC, 매뉴얼, 논문.
- **도식 바로 아래에 출처를 한 줄로 남긴다.** 섹션 번호나 앵커까지 적는다.

  ```markdown
  ![B-Tree 인덱스 탐색 경로](fig/btree-lookup.svg)

  > **출처**: [SQLite Database File Format §1.6 B-tree Pages](https://www.sqlite.org/fileformat2.html#b_tree_pages)
  ```

  `> **출처**:` 형식을 그대로 쓴다. `구조 근거`, `규약 근거` 같은 변형을 쓰지 않는다.
  `blog.py lint`가 이 형식으로 도식 수와 출처 수를 맞춰 센다.

- 여러 자료를 합쳐 그렸으면 전부 적는다. 근거를 못 찾은 부분은 도식에 넣지 않는다.
- 같은 출처를 **참고 자료** 섹션에도 중복해 적는다. 도식 아래 한 줄은 그림의 출처,
  참고 자료는 글 전체의 출처다.

## 5. 버전 명시 (모든 글)

**독자가 "내 버전에서도 그런가"를 판단할 수 있어야 한다.** 버전 없는 기술 글은 혼동만 만든다.

- 프론트매터 `environment`에 쓴 도구·DB·언어의 **버전을 숫자까지** 적는다.
  `["SQLite 3.49.1", "Python 3.13.5"]` (O) / `["SQLite", "Python"]` (X)
- 버전은 실제로 확인한 값을 적는다. `sqlite3.sqlite_version`, `git --version`,
  `SELECT * FROM v$version` 처럼 직접 뽑아 확인한다.
- **제품 글은 제목에도 버전을 넣는다.** "Tibero 7 시퀀스 — …"
- 본문에서 버전에 따라 동작이 갈리는 부분은 그 사실을 적는다.

`blog.py lint`가 `environment` 유무와 버전 숫자 포함 여부를 검사한다.

## 6. 제품 시리즈 글 (`track: product`)

DB 제품의 기능 하나를 골라 사용법과 문법을 정리한다.

### 실행 환경

원격 Tibero가 SSH로 붙어 있으면 제품 글도 실제로 돌려 `executed`로 쓴다.
접속 정보는 gitignore된 로컬 설정에 두고 저장소에 올리지 않는다. 검증할 때는
**빈 스키마에 예제 객체만 만들고 끝나면 전부 지운다.** 기존 객체는 조회조차 하지 않는다.

출력에서 호스트명·파일 경로·IP·내부 빌드 번호를 지운다. 공개된 버전 표기는 남긴다.

### 제품 순환

한 제품의 기능을 다 다루면 다음 제품으로 넘어간다. 순서는
`topics/backlog.yaml`의 `meta.product_rotation`에 있다.

> Tibero 7 → Oracle 19c → MySQL 8.0 → PostgreSQL 16

**제품을 옮길 때 `feature` 키를 똑같이 쓴다.** 이게 이 트랙의 핵심이다.
같은 키를 쓰면 `blog.py relink`가 제품 간 상호 링크를 만들고, 비교 절(§8)이 붙는다.
제품이 하나뿐이면 이 구조는 아무 값도 못 한다.

다음 제품 주제를 백로그에 넣을 때는 **앞 제품의 feature 목록을 그대로 복사해** 쓴다.
그 제품에 없는 기능이면 "없다"는 것 자체가 좋은 비교 소재이므로 주제로 남긴다.

### 검증 방식 — `verification`

| 값 | 의미 | 조건 |
|---|---|---|
| `executed` | 실제로 실행해 출력을 확인 | 그 환경이 있을 때만 |
| `manual-only` | 공식 매뉴얼 근거만 | 실행 환경이 없을 때 |

실행 환경 판별은 간단하다. 해당 제품의 클라이언트(`tbsql`, `sqlplus`, `mysql`, `psql`)가
PATH에 있으면 실행 검증, 없으면 매뉴얼 근거다.

`manual-only`일 때 지킬 것:

- 본문 상단에 **"실행 검증 없음"** 문구가 들어간 안내를 넣는다 (lint가 검사한다)
- **결과표·출력·측정값을 절대 지어내지 않는다.** 문법과 매뉴얼에 적힌 동작만 쓴다
- 문장마다 매뉴얼 근거가 있어야 한다. 근거 없는 동작 설명은 쓰지 않는다
- 나중에 실행 환경에서 예제를 돌려 출력을 채우면 `executed`로 올린다

### 회사 정보 취급 — 예외 없음

이 저장소는 **공개**다. 특히 Tibero 글을 쓸 때 다음은 넣지 않는다.

- IMS·패치 번호, 내부 이슈 번호, 사내 문서 인용
- 고객사 이름, 고객 환경, 장애 사례
- 미출시·미공개 기능, 내부 테스트 결과, 비공개 벤치마크
- 사내에서만 접근 가능한 도구·경로·호스트명

**공개 매뉴얼에 있는 내용만 쓴다.** 판단이 서지 않으면 쓰지 않는다.

## 7. 정보관리기술사 글 (`track: pe`)

시험 과목의 기술 개념·정의를 다룬다. 읽는 사람은 **답안에 쓸 수 있는 형태**를 원한다.

### 글 구조 — 이 트랙만 다르다

§2의 고정 템플릿 대신 기술사 답안 구조를 쓴다.

1. **들어가며** — 이 개념이 왜 시험에 나오는지, 실무에서 어떤 문제를 푸는지. 3~4문장
2. **정의** — 한 문장으로 끊어지는 정의. 답안 첫 줄에 그대로 쓸 수 있어야 한다
3. **등장 배경** — 이전 방식의 무엇이 문제였는지
4. **구성요소 / 절차** — 항목을 빠짐없이. 개수를 명시한다 ("4단계", "6가지")
5. **도식** — 이 트랙에서 특히 중요하다. 답안에 그릴 수 있을 만큼 단순해야 한다
6. **비교** — 유사 개념과의 대비 표. 시험에서 가장 자주 묻는 형태다
7. **적용 시 고려사항** — 실무에서 무엇을 판단해야 하는지. 최소 3개
8. **정리** — 암기 단서. 두문자나 개수로 묶어준다
9. **참고 자료** — 표준 문서·공식 자료 우선

### 지킬 것

- **정의는 출처 있는 것을 쓴다.** ISO/IEEE 표준, 공식 가이드, 원 논문 순으로 우선한다.
  출처마다 정의가 다르면 그 사실을 밝히고 둘 다 적는다
- **항목 개수를 반드시 명시한다.** 답안에서 개수가 곧 점수다
- **도식은 손으로 그릴 수 있어야 한다.** 답안지에 5분 안에 옮길 수 없으면 너무 복잡한 것이다
- **`manual-only`가 기본이다.** 실행 예제가 없으므로 본문에 "실행 검증 없음"을 밝힌다.
  단, 알고리즘·계산 문제(스케줄링, 페이지 교체, 정규화)는 **직접 계산해 검산하고**
  그 과정을 실을 수 있다. 이때는 `executed`로 올린다
- **수치를 지어내지 않는다.** 성능 수치나 시장 통계를 근거 없이 쓰지 않는다
- 시험 범위라고 해서 얕게 쓰지 않는다. "왜 그렇게 설계됐는지"가 빠지면 암기장일 뿐이다

## 8. 같은 기능을 다른 제품에서 다룰 때

프론트매터 `feature` 키가 제품을 넘어 같은 기능을 잇는다.

1. 글을 쓰기 전 `python scripts/blog.py related <feature>`로 같은 키의 글이 있는지 본다
2. 있으면 **`## 다른 환경에서는`** 절을 넣어 비교한다. 문법 차이, 기본값 차이,
   지원 여부 차이를 표로 정리하고 각각 어느 버전 기준인지 밝힌다
3. 글을 다 쓴 뒤 `python scripts/blog.py relink`를 돌린다.
   같은 키의 글들끼리 서로를 가리키는 블록이 자동으로 생기고, **기존 글에도 역링크가 생긴다**

`relink`가 만드는 블록은 손으로 고치지 않는다. 매번 재생성된다.

```markdown
<!-- related:start -->
> **같은 기능을 다른 환경에서 다룬 글** (`transaction-isolation`)
> - [제목](../../sqlite/2026-09-20-.../index.md) — SQLite 3.49.1
<!-- related:end -->
```

비교할 때 **한쪽이 우월하다는 식으로 쓰지 않는다.** 설계 선택이 다른 것이고,
어느 쪽이 어떤 상황에 맞는지를 적는다.

## 9. 프론트매터 (Jekyll/Hugo 호환)

일반 글:

```yaml
---
title: "글 제목"
date: 2026-09-18
categories: [Database]                          # 최상위 1개
subcategory: sqlite                             # 제품·도구에 묶인 주제만
tags: [index, btree, sqlite]                    # 3~6개, 소문자
description: "한 줄 요약. 검색 결과에 노출되는 문장."
difficulty: intermediate                        # beginner | intermediate | advanced
feature: index-usage                            # 제품 간 비교를 잇는 키 (없으면 생략)
environment: ["SQLite 3.49.1", "Python 3.13.5"] # 버전 숫자까지 필수
verification: executed                          # executed | manual-only
verified: true                                  # 검증 완료 여부
topic_id: db-001
---
```

제품 시리즈 글 — `product`, `product_version`이 추가된다:

```yaml
---
title: "Tibero 7 시퀀스 — 생성, 캐시, 그리고 값이 튀는 순간"
date: 2026-09-19
categories: [Database]
subcategory: tibero
tags: [tibero, sequence, sql]
description: "..."
difficulty: beginner
product: Tibero
product_version: "7"
feature: sequence
environment: ["Tibero 7"]
verification: manual-only
verified: true
topic_id: tb-001
---
```

## 10. 1차 자료가 없는 최신 주제

기술사 1교시에는 **공식 문서가 아직 없는 주제**가 나온다. 다크 팩토리, Physical AI,
하네스 엔지니어링, 비인간 신원(NHI) 같은 것들이다. 표준도 벤더 문서도 없고, 있는 것은
컨설팅사 리포트와 기술 블로그뿐인 경우가 많다.

이럴 때 **주제를 건너뛰지 않는다.** 대신 출처 등급을 낮춰 쓰되, 낮췄다는 사실을 밝힌다.

### 출처 등급

| 등급 | 무엇 | 취급 |
|---|---|---|
| **1차** | 표준(ISO·IEEE·RFC·NIST), 공식 문서·매뉴얼, 논문, 정부·공공기관 발간물 | 있으면 무조건 이것을 쓴다 |
| **2차** | 기업 엔지니어링 블로그, 애널리스트 리포트, 업계 매체 기사 | 1차가 없을 때만 |
| **쓰지 않음** | 출처 없는 요약 글, 서로 베낀 게시물, 생성형 AI가 쓴 티가 나는 글 | 근거로 쓰지 않는다 |

### 2차 자료로 쓸 때 지킬 것

1. **1차 자료를 먼저 찾는다.** 용어의 영문 원어, 표준화 기구 이름, 제안한 기업 이름으로
   각각 검색한다. 한글 용어만으로 검색하고 없다고 판단하지 않는다.
2. **독립된 출처 3개 이상**에서 같은 설명이 확인돼야 쓴다. 같은 원문을 옮긴 글들은
   **하나로 센다.** 세 글이 같은 컨설팅 리포트를 인용하고 있으면 출처는 1개다.
3. **출처마다 발행일을 적는다.** 최신 주제는 1년 전 글이 이미 틀렸을 수 있다.
   2년 넘은 2차 자료만 나오면 그 사실을 본문에 밝힌다.
4. **본문 상단에 밝힌다** — `> 1차 자료 없음. 2차 자료 3건을 교차 확인해 정리했다.`
5. **정의가 엇갈리면 통일하지 않는다.** 출처별로 다르게 쓴다는 사실 자체를 적고 둘 다 싣는다.
   시험 답안에서도 "정의가 정립되는 중"은 쓸 수 있는 문장이다.
6. **수치는 원 출처가 1차일 때만 쓴다.** 블로그가 인용한 시장 규모·성능 수치는 원 출처를
   따라가 확인하고, 못 찾으면 **쓰지 않는다.** "n배 빠르다"는 근거 없이 쓰지 않는다.
7. **도식은 개념 관계도까지만.** 2차 자료만으로 내부 구조나 프로토콜 흐름을 그리지 않는다.
   구성요소와 그 관계를 묶는 분류도·관계도는 괜찮다. 도식 아래 출처 줄에
   `(2차 자료 기반)`을 붙인다.

독립 출처 3개를 못 채우면 그 주제는 `todo`로 되돌리고 **이유를 백로그에 적는다.**
근거가 모자라 못 썼다는 기록이, 억지로 쓴 글보다 낫다.

## 11. 기출문제 풀이 (`track: exam`)

정보관리기술사 기출문제를 매일 오후 3시에 푼다. 개념을 정리하는 `pe` 트랙과 **다르다.**
`pe`는 "이 기술이 무엇인가"를 쓰고, `exam`은 **"답안지에 이렇게 쓴다"**를 쓴다.

글은 `posts/PE/exam/<날짜>-<slug>/index.md`에 둔다.

### 한 회차에 몇 문제

| 구분 | 배점 | 한 회차에 |
|---|---|---|
| 단답형 (1교시) | 10점 | **2문제** |
| 논술형 (2~4교시) | 25점 | **1문제** |

`python scripts/blog.py exam-pick`이 다음 문제를 정한다. 최신 회차부터, 교시 순서대로
내려간다. 손으로 고르지 않는다.

### 표기 규칙 — 반드시 지킨다

- **회차와 문제 번호를 글에 쓰지 않는다.** 제목·본문·프론트매터 어디에도 쓰지 않는다.
  `기출문제`라고만 쓴다.
- **문제 원문을 그대로 옮기지 않는다.** 무엇을 묻는지 내 문장으로 다시 쓴다.
- **문제 데이터는 저장소 밖에 둔다.** 문제지 PDF와 `exam-questions.yaml`은
  `tech_blog_exam_src/`(저장소 바깥)에 있다. 공공누리는 제140회부터 적용되므로
  그 이전 회차의 문제 원문을 공개 저장소에 올리지 않는다.
  경로는 환경변수 `EXAM_SRC_DIR`로 바꿀 수 있고, 기본값은 저장소 옆의 `tech_blog_exam_src`다.

### 발췌 기준 — IT 쪽에서 가져온다

정보관리기술사는 **정보통신 분야**의 자격이다. 문제가 제조(다크 팩토리), 경영(BPR·ESG),
물류를 다루더라도 **답안의 축은 정보·시스템·데이터**여야 한다. 도메인 그 자체를 설명하면
그 분야 기술사의 답안이지 정보관리기술사의 답안이 아니다.

- **도메인 지식은 배경으로만 쓴다.** 점수가 붙는 곳은 "그 도메인의 문제를 정보기술로
  어떻게 푸는가"다. 어떤 데이터를 모으고, 어떤 시스템이 어떻게 연계되고,
  무엇을 표준으로 맞추는가.
- **출처도 IT 표준을 1순위로 둔다.** ISO/IEC, IEEE, RFC, NIST, IEC 62264(ISA-95),
  IEC 62443처럼 정보·제어·연계를 다루는 표준이 뼈대가 된다.
- **도메인 표준은 제약 조건으로만 인용한다.** 예를 들어 로봇 안전 규격(ISO 10218)은
  "무인 운전이 성립하려면 이것이 전제된다"는 제약으로 쓰고, 답안의 뼈대로 쓰지 않는다.
- **판별법**: 답안에서 정보시스템 이야기를 걷어냈을 때 **글이 그대로 남으면 잘못 쓴 것이다.**
  걷어내면 뼈대가 무너져야 IT 답안이다.

경영·정책 문항(거버넌스, 감리, 법제)도 같다. 제도 설명에서 그치지 말고 그 제도가
**정보시스템에 무엇을 요구하는지**까지 내려간다.

### 개념 글(`pe`)과의 관계 — 섞지 않는다

`exam`은 **답안**이고 `pe`는 **개념 글**이다. 한 글에 둘을 섞으면 답안은 늘어지고
개념은 토막난다.

- 답안을 쓰다 **블로그에 없는 개념**을 만나면, 답안은 답안대로 끝내고 개념은 백로그에 넣는다:

  ```bash
  PYTHONUTF8=1 python scripts/blog.py add-topic \
    --track pe --title "<개념 이름>" --subcategory <분야> \
    --tags "정보관리기술사,<분야>,개념정리" \
    --angle "<어느 각도로 쓸지>" --origin exam
  ```

  **오후 5시 개념 루틴**이 `blog.py pick-concepts`로 이것을 먼저 집어 간다.
  **오후 3시 루틴이 개념 글까지 쓰지 않는다.** 한 회차에 다 하면 둘 다 얕아진다.

- 개념 글이 **이미 있으면** 답안 끝(`끝` 위)에 링크 한 줄을 넣는다:

  ```markdown
  > 개념 정리: [제목](../../software-engineering/2026-09-18-<slug>/index.md)
  ```

- 반대로 `pe` 개념 글을 쓸 때 같은 주제의 기출 답안이 이미 있으면 그쪽으로도 링크한다.
  답안에서 개념으로, 개념에서 답안으로 양쪽이 이어져야 쓸모가 있다.

- 개념이 **이미 백로그에 있거나 발행됐으면** `add-topic`이 알아서 걸러낸다. 중복 등록을
  걱정하지 말고 그냥 돌린다.

### 답안 구조

실제 답안지 형식을 따른다. 채점자가 훑어서 항목을 셀 수 있어야 한다.

```
Ⅰ. <제목> — 대항목. 로마 숫자
  가. <중항목>
    ① <소항목>
```

- **단답형**: `Ⅰ. 정의` → `Ⅱ. 구성요소(또는 특징)` → `Ⅲ. 활용/고려사항`.
  A4 한 장 분량. 모식도 1개.
- **논술형**: `Ⅰ. 개요` → `Ⅱ.`~`Ⅳ.` 문항이 요구한 항목 순서 그대로 → `Ⅴ. 결론`.
  A4 세 장 분량. 모식도 최소 1개, 비교표 최소 1개.
- **문항이 "가. 나. 다."로 나눠 물으면 그 순서를 그대로 대항목으로 쓴다.** 순서를 바꾸지 않는다.
- 답안 마지막 줄은 `끝`으로 닫는다.
- 모식도는 §4의 SVG 규칙을 그대로 따르되, **답안지에 5분 안에 옮겨 그릴 수 있어야 한다.**
  박스 8개를 넘지 않는다.
- 답안 뒤에 **`## 답안 작성 메모`**를 붙인다. 무엇을 먼저 쓸지, 시간이 모자라면 무엇을
  버릴지, 이 문제에서 점수가 갈리는 지점이 어디인지. 답안 자체와 섞지 않는다.
- 출처는 §10 등급을 따른다. 최신 주제라 1차 자료가 없으면 2차 교차 확인으로 쓰고 밝힌다.

## 12. 하지 말 것

- 제목에 "완벽 정리", "총정리", "한 방에", "끝판왕" 사용 금지
- AI가 썼음을 드러내는 상투구 ("~에 대해 알아보겠습니다", "핵심을 짚어보자면") 금지
- 같은 주제를 다른 제목으로 재탕 금지 → `topics/published.md`를 먼저 확인
- 원문 15단어를 넘는 직접 인용 금지 (저작권)
- **버전 없는 기술 글 금지** → §5
- **회사 내부 정보 금지** (IMS 번호, 고객사, 미출시 기능, 사내 경로) → §6
- **일반 트랙**: 공식 문서를 요약만 한 글 금지. 반드시 실험·측정·코드가 들어간다
- **제품 트랙**: 실행 환경이 없으면 매뉴얼 근거 정리가 맞다. 대신 §6을 지킨다 —
  "실행 검증 없음"을 밝히고, **출력·측정값을 절대 지어내지 않는다**.
  단순 나열로 끝내지 말고 "어느 상황에 무엇을 쓰는가", "어디서 틀리기 쉬운가"를 넣는다
- **기술사 트랙**: 출처 없는 정의 금지, 항목 개수 누락 금지, 근거 없는 수치 금지 → §7
- **기출 트랙**: 회차·문제 번호 표기 금지, 문제 원문 전재 금지 → §11
- **1차 자료가 없다고 건너뛰지 않는다.** 2차 자료 3건 교차 확인으로 쓰고 밝힌다 → §10
