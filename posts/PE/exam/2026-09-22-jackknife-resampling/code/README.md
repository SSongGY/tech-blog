# 잭나이프 재표집 검산

답안에 실은 수식을 다섯 개짜리 표본에서 직접 계산해 확인한다. 외부 의존성은 없다.

- `[1]` 편향 보정값이 불편분산 `S^2` 와 **정확히** 같은지
- `[2]` 표본평균에 대한 잭나이프 분산이 `S^2/n` 과 같은지
- `[3]` 추정량이 중앙값일 때 복제값이 몇 가지 값만 갖는지

부동소수점 오차로 "거의 같다"가 되지 않도록 `fractions.Fraction` 으로 계산한다.

## 실행

```bash
python jackknife.py
```
