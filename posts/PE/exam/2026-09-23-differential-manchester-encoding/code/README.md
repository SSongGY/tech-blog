# 차등 맨체스터 부호화 검산

답안에 적은 부호화 규칙을 8비트 입력으로 직접 돌려 확인한다. 외부 의존성은 없다.

- 두 규약(IEEE 802.5, IEEE 802.3cg 10BASE-T1S)의 파형과 비트당 천이 수
- `[1]` 선로 극성을 뒤집어도 같은 비트로 복호되는지
- `[2]` 두 규약이 같은 비트열에 다른 파형을 내는지
- `[3]` 802.5 시작 구분자의 J·K 심벌이 데이터 비트와 어떻게 구별되는지

## 실행

```bash
python dme.py
```
